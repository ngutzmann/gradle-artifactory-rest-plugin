apply plugin: 'groovy'

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'org.codehaus.groovy', name: 'groovy', version: '2.3.2'
    compile group: 'org.jfrog.artifactory.client', name: 'artifactory-java-client-api', version: '0.14'
}
